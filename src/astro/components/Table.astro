---
/**
 * @since 0.1.0-alpha
 *
 * @packageDocumentation
 */

/* import - types */
import type { HTMLAttributes } from 'astro/types';
import type { GenericElementProps } from '../../ts/types/index.js';

/* import - functions */

/* import - values */

/* import - components */

/* import - layouts */

/* TYPES ===================================== */

/**
 * Input props for the Table component.
 *
 * @since 0.1.0-alpha
 * @since 0.1.0-alpha.17 — Moved to component file.
 */
export interface TableProps {
    wrapper?: HTMLAttributes<'div'>;
}

/* PROPS ===================================== */

type Props = GenericElementProps<TableProps, 'div'>;

const {
    class: classList = [],
    wrapper = {},
    ...attributes
}: Props = Astro.props.frontmatter ?? Astro.props;

/* CONSTANTS ===================================== */
---

<div
    class:list={['table-wrapper', '||', [wrapper.class]]}
    {...wrapper}
>
    <table
        class:list={[classList]}
        {...attributes}
    >
        <slot />
    </table>
</div>

<style>
    .table-wrapper {
        > table {
            min-width: 100%;
        }
    }
</style>
