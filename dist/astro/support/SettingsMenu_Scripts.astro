---
/**
 * @since 0.1.0-alpha
 *
 * @packageDocumentation
 */

/* import - types */

/* import - functions */
import Toggle_Scripts from './Toggle_Scripts.astro';

/* import - values */

/* import - components */

/* import - layouts */

/* TYPES ===================================== */

/* PROPS ===================================== */

/* CONSTANTS ===================================== */
---

<Toggle_Scripts />

<script>
    import { SettingsMenu } from '../../ts/classes/SettingsMenu.js';

    const settingsMenus: NodeListOf<HTMLElement> = document.querySelectorAll(
        '[data-settings-menu]',
    );

    const scrollBehaviour =
        (window.getComputedStyle(document.documentElement).scrollBehavior as
            | ScrollBehavior
            | undefined) || undefined;

    /*
     * Setting up each found menu.
     */
    SettingsMenu.init(settingsMenus, scrollBehaviour);
</script>
