---
/**
 * @since 0.1.0-alpha.draft
 *
 * @packageDocumentation
 */

/* import - types */
import type { GenericElementProps } from '../../ts/00-types/index.js';
import type { Props as AlertProps } from './Alert.d.ts';

/* import - functions */

/* import - values */

/* import - components */
import Icon from './Icon.astro';

/* import - layouts */

/* TYPES ===================================== */

export type * from './Alert.d.ts';

/* PROPS ===================================== */

export interface Props extends GenericElementProps<AlertProps, 'div'> {}

const {
    type,
    class: classList = [],
    ...attributes
}: Props = Astro.props.frontmatter ?? Astro.props;

/* CONSTANTS ===================================== */
---

<div
    class:list={['alert-block', classList]}
    {...attributes}
>
    <Icon type={type} />
    <slot name='headline' />
    <slot />
</div>
